<%= content_for(:title, "Blog") %>
<div class="row medium-8 large-7 columns">
	<% @posts.each do |p| %>
		<div class="blog-post">
			<h3><%= p.title %></h3>
			<h4><%= p.author %> | <small><%= p.updated_at %></small></h4>
			<p><%= p.body %></p>
		</div>
	<% end %>
</div>
