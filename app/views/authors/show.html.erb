<%= content_for(:title, @author.name) %>
<div class="medium-8 large-7 columns" id="author">
	<%= render @author %>
	<% @author.posts.each do |p| %>
		<div class="blog-post">
			<%= render p %>
		</div>
	<% end %>
</div>
